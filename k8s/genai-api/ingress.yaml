apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: genai-api-ingress
spec:
  rules:
    - host: genai.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: genai-api-service
                port:
                  number: 80
